/** Wordmark at the left edge of the header: bold CAD, then the accent-orange suffix. */
export function AppTitle() {
  return (
    <h1 className="font-mono text-[11px] uppercase tracking-[0.14em]">
      <span className="font-bold text-white">CAD</span>
      <span className="font-normal text-[#f4902c]">.LIKE.AUDIO</span>
    </h1>
  );
}
