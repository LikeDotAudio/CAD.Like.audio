/** Wordmark at the left edge of the header. */
export function AppTitle() {
  return (
    <h1 className="font-mono text-[11px] font-bold uppercase tracking-[0.14em] text-white">
      CAD.LIKE.AUDIO
    </h1>
  );
}
